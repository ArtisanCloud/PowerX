---
name: sts-ruleset
description: PowerX STS ruleset（当前源可为空，保留占位）。
---

# PowerX STS Ruleset

## 步骤

1) 打开 `本文件内嵌规则`。
2) 按规则执行实现/校对。
3) 完成后按核对清单验收。

## 核对点

- 与 PowerX 当前代码结构、路径与命名一致。
- 仅在传输层/契约层做职责内改动，不跨层越界。

## 规则（内嵌）

### sts.yaml

````yaml
# TODO: 源文件当前为空，请补齐规则后再执行严格校验。
````
