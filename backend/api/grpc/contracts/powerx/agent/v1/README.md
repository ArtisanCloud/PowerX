# Agent Lifecycle Contracts

This directory houses the `powerx.agent.v1` protobuf definitions for agent registration, lifecycle control, and observability contracts.  
The concrete `agent_lifecycle.proto` file will be added in later phases; keeping this README ensures the package directory is tracked and documented.
